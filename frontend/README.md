# InduSense Frontend

Interface Vite + React + MUI pour piloter le backend FastAPI InduSense.

```powershell
npm install
npm run dev
```

Le serveur Vite proxyfie `/api` vers `http://127.0.0.1:8000`.
